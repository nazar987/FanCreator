# dependencies
node_modules

# logs
*.log

# electron-vite build output (anchored to repo root so nested src dirs are not ignored)
/out
.vite

# packaged builds
/dist

# legacy nextron build (removed)
.next
/app
