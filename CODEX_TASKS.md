# CODEX_TASKS — задачи для Codex (junior)

> Этот файл ведёт senior (Claude). Codex берёт задачи **сверху вниз**, по одной.
> Перед началом задачи прочитай разделы «Золотые правила» и «Шпаргалка по архитектуре».
> Не уверен — **не угадывай**, оставь комментарий `// TODO(senior): вопрос ...` и переходи дальше.

---

## 🟡 Золотые правила (читать каждый раз)

1. **Язык интерфейса — русский.** Все надписи, кнопки, подсказки — по-русски.
2. **TypeScript строгий.** Перед тем как считать задачу готовой, запусти `npm run typecheck` —
   должно быть **0 ошибок**. Если не проходит — чини, пока не пройдёт.
3. **Не трогай редактор.** Файлы `src/renderer/src/features/editor/**` и пагинацию —
   это зона senior. Если задача якобы требует туда лезть — стоп, спроси senior.
4. **Не добавляй новые npm-зависимости** без разрешения senior. Используй то, что уже есть
   (`lucide-react`, `@hello-pangea/dnd`, `framer-motion` уже установлены).
5. **Никаких `window.prompt/confirm/alert`** — в Electron они заблокированы. Используй
   `promptText()` и `confirmDialog()` из `src/renderer/src/shared/ui/dialogs.tsx`.
6. **Стиль кода — как в соседних файлах.** Те же отступы, именование,組 структура. Цвета —
   только через CSS-переменные (`var(--accent)`, `var(--panel)`, `var(--text)` и т.д.,
   см. `src/renderer/src/theme/theme.css`). Никаких хардкод-цветов типа `#fff`.
7. **Одна задача — один коммит.** Сообщение коммита на русском, кратко по сути.
8. После изменения данных в renderer **всегда** обновляй состояние через `applyProject(result)`
   (см. шпаргалку). Не дублируй логику стора.

---

## 🧭 Шпаргалка по архитектуре

Стек: **Electron + electron-vite + React 18 + TypeScript**. Данные — на диске
(`userData/FanCreator/projects/<id>/project.json`), картинки — файлами, отдаются по протоколу
`asset://`. UI — свой дизайн-система на CSS-переменных (без Mantine).

### Поток данных (главное!)
```
renderer (UI)  --window.api.*-->  preload  --ipcRenderer.invoke-->  main (ipc handlers)  -->  диск
```
Почти все мутации возвращают **полный обновлённый `Project`**. В renderer после вызова делай:
```ts
const { current, applyProject } = useStore()
const result = await window.api.stories.update({ projectId: current.id, storyId, patch })
applyProject(result)   // обновит и текущий проект, и список на главном экране
```

### Как добавить новый IPC-метод (если задача требует)
Нужно поправить **3 файла** синхронно:
1. `src/main/ipc/index.ts` — сам обработчик. Используй готовый помощник
   `mutate(projectId, (p) => { ... })` — он читает проект, применяет мутацию, пишет на диск
   и возвращает проект. Есть утилиты `now()`, `uid()`.
2. `src/shared/api.ts` — добавь сигнатуру метода в интерфейс `FanCreatorApi` (типобезопасно).
3. `src/preload/index.ts` — пробрось: `methodName: (input) => invoke('channel:name', input)`.

Каналы именуются `сущность:действие` (`stories:update`, `characters:add` …).

### Готовые кирпичики UI (переиспользуй, не пиши заново)
- `src/renderer/src/shared/ui/components.tsx`: `Button` (variant `primary|ghost|soft|danger`,
  size `sm|md`, `icon`), `Input` (есть проп `icon`), `Card`, `StatusBadge`, `Hashtags`.
- `src/renderer/src/shared/ui/dialogs.tsx`: `promptText({title, placeholder?, initial?})` →
  `Promise<string|null>`; `confirmDialog({title, message?, danger?})` → `Promise<boolean>`.
- `src/renderer/src/shared/ui/ContextMenu.tsx`: `openContextMenu(event, items: MenuItem[])`.
  `MenuItem = { label, icon?, onClick?, danger?, submenu?, type?: 'sep'|'label' }`.
- Иконки: `lucide-react` (`import { Plus, Trash2 } from 'lucide-react'`).

### Стор (`src/renderer/src/store/store.tsx`) — что доступно через `useStore()`
`current` (открытый Project | null), `applyProject(p)`, `reloadCurrent()`,
`projects` (список сводок), `refreshProjects()`, `openProject(id)`, `closeProject()`,
`tabs`, `activeTabId`, `openTab({id?, kind, title, storyId?, chapterId?})`, `closeTab(id)`,
`setActiveTab(id)`, `theme`, `setTheme(t)`.
Тип вкладки `OpenTab.kind` уже включает `'shelf' | 'chapter' | 'characters'`.

### Модель данных — `src/shared/types.ts`
`Project { id, title, coverPath, description, tags[], theme, stories[], characters[] }`.
`Story { id, title, coverPath, synopsis, tags[], genres[], status, chapters[] }`.
`Character { id, name, role, tags[], templateId, fields: CharacterField[], avatarPath }`.
`CharacterField { id, label, value }`. `ChapterStatus = 'idea'|'draft'|'editing'|'done'`.

### Где что рисуется
- `src/renderer/src/app/App.tsx` — выбирает контент по `active.kind`.
- `src/renderer/src/app/Sidebar.tsx` — дерево историй/глав, контекстные меню.
- `src/renderer/src/app/TabBar.tsx` — верхние вкладки и меню «+».
- `src/renderer/src/features/library/**` — главный экран и полка с обложками.
- CSS каждой фичи импортируется в `src/renderer/src/main.tsx`.

### Definition of Done (для каждой задачи)
- [ ] `npm run typecheck` — без ошибок.
- [ ] `npm run dev` — приложение запускается, фича работает, данные переживают перезапуск.
- [ ] Интерфейс на русском, цвета через переменные, использованы готовые кирпичики UI.
- [ ] Не задеты редактор и пагинация.

---

# ЗАДАЧИ (по порядку)

## ✅ T1 — Раздел «Персонажи» (Medium) — СДЕЛАНО (commit 620b680, ревью senior пройдено)
> Следующая задача — **T2**. T2 также уберёт `TODO(senior)` про TagEditor в карточке персонажа.

### (архив описания T1)
**Зачем:** пожелание №16. В новом UI раздела персонажей пока **нет вообще** (есть только модель
и IPC). Нужно собрать экран.

**IPC уже готов** — ничего в main/preload менять не нужно:
`window.api.characters.add({projectId, name})`,
`.update({projectId, characterId, patch})` (patch принимает `name, role, tags, fields`),
`.delete({projectId, characterId})`. Все возвращают `Project`.

**Файлы:**
- создать `src/renderer/src/features/characters/Characters.tsx` и `characters.css`;
- импортировать css в `src/renderer/src/main.tsx`;
- в `src/renderer/src/app/App.tsx` отрисовать `<Characters />` когда `active?.kind === 'characters'`;
- в `src/renderer/src/app/TabBar.tsx` в меню «+» (`addMenu`) добавить пункт «Персонажи»,
  открывающий `openTab({ id: 'characters', kind: 'characters', title: 'Персонажи' })`.

**Что должно быть на экране:**
- Кнопка «Добавить персонажа» (`characters.add`, имя по умолчанию «Новый персонаж»).
- Карточки персонажей (используй `Card`). В карточке:
  - Имя — `Input`, сохранение на `onBlur` (patch `{name}`).
  - Роль — `Input` (patch `{role}`).
  - Теги — компонент `TagEditor` из задачи **T2** (если T2 ещё не сделана, временно покажи
    `Hashtags` только на чтение и оставь `// TODO(senior): подключить TagEditor после T2`).
  - Список произвольных полей `fields` (биография, внешность и т.п.): каждое поле = подпись
    (`label`) + многострочный текст (`<textarea>`-стилизуй под `.input`). Сохранение на blur
    обновляет массив `fields` целиком в patch `{fields}`.
  - Кнопка **«+ Подробнее»** — спрашивает `promptText({title:'Название характеристики'})`,
    добавляет в `fields` новый объект `{ id: crypto.randomUUID(), label, value: '' }`.
  - У каждого поля — крестик удаления (убрать из `fields`).
  - Кнопка удаления персонажа (через `confirmDialog`, `danger:true`).
- Всё сохраняется через `applyProject(result)`.

**Приёмка:** можно добавлять/редактировать/удалять персонажей и их поля; после перезапуска
всё на месте; вкладка «Персонажи» открывается из «+».

**НЕ делать:** шаблоны анкет и применение к группе — это отдельная задача senior. Здесь только
ручное редактирование одного персонажа.

---

## ✅ T2 — TagEditor + теги/жанры/свойства истории (Easy) — СДЕЛАНО (commit aaf8e51, ревью senior пройдено)
> TagEditor подключён и в карточку персонажа (TODO из T1 закрыт). Следующая задача — **T3**.

### (архив описания T2)
**Зачем:** пожелания №3 и №19 (хэштеги, жанры).

**Часть A. Переиспользуемый редактор тегов.**
Создать `src/renderer/src/shared/ui/TagEditor.tsx`:
```ts
function TagEditor(props: { tags: string[]; onChange: (tags: string[]) => void; placeholder?: string }): JSX.Element
```
- Поле ввода; **Enter** добавляет тег (без `#`, без дублей, trim); **Backspace** на пустом поле
  удаляет последний; у каждого чипа крестик. Стили — в `ui.css` (класс `.tag-chip`),
  цвета через переменные.

**Часть B. Окно «Свойства истории».**
Создать `src/renderer/src/features/library/StoryProperties.tsx` — модалка (по образцу
`.modal` из `ui.css`, можно как в `dialogs.tsx`) с полями: название (`Input`), синопсис
(`textarea`), статус (`<select>` со значениями ChapterStatus + русские подписи — есть
`STATUS_LABEL` в `components.tsx`), теги (`TagEditor`), жанры (`TagEditor`). Кнопка «Сохранить»
вызывает `window.api.stories.update({ projectId, storyId, patch })` → `applyProject`.

Открывать это окно из контекстного меню истории: в `src/renderer/src/app/Sidebar.tsx`
в `storyMenu(s)` добавить пункт «Свойства» (иконка `Settings2`). Хранить открытую историю в
локальном `useState` Sidebar и рендерить `<StoryProperties .../>` при наличии.

**Приёмка:** у истории можно задать синопсис, статус, теги и жанры; теги/жанры видны в дереве
и на полке как `#хэштеги`; сохраняется после перезапуска. После T2 — подключить `TagEditor`
в карточку персонажа (T1) и оставить там `tags` редактируемыми.

---

## ✅ T3 — Сохранение темы оформления по проекту (Easy) — СДЕЛАНО (commit f67e998)
### (архив описания T3)
**Зачем:** пожелание №12 — тема должна запоминаться.
Сейчас `openProject` применяет `project.theme`, но смена темы нигде не сохраняется.

**Файл:** `src/renderer/src/store/store.tsx`, функция `setTheme`.
Доработать так: если открыт проект (`current` не null) — после смены темы сохранить её:
`window.api.projects.update({ projectId: current.id, patch: { theme: t } })` и затем
`applyProject(result)`. На главном экране (без проекта) — просто менять локально, как сейчас.

**Приёмка:** открыть проект, сменить тему, закрыть и открыть проект снова → тема та же.
На разных проектах могут быть разные темы.

---

## ✅ T4 — Обложка проекта на главном экране (Easy-Medium) — СДЕЛАНО (commit fd7c5f7)
### (архив описания T4)
**Зачем:** пожелание №19 (проекты тоже как книги).
Сейчас обложки есть только у историй. Нужно повторить для проектов.

**IPC — добавить (3 файла, см. шпаргалку), по образцу `stories:setCover`/`stories:pickCover`:**
- `projects:setCover` `{ projectId, source, isDataUrl? }` — сохраняет картинку (используй уже
  существующие `saveAsset`, `assetUrl`, `dataUrlToBuffer` в `src/main/ipc/index.ts`) и пишет
  результат в `p.coverPath`.
- `projects:pickCover` `{ projectId }` — диалог выбора файла (как у историй).
Добавь обе в `FanCreatorApi.projects` (`src/shared/api.ts`) и в `src/preload/index.ts`.

**Renderer:** `src/renderer/src/features/library/Home.tsx` — передать в `<CoverArt>` пропсы
`onDropImage` (→ `projects.setCover` с `isDataUrl:true`) и `onPick` (→ `projects.pickCover`),
после вызова — `refreshProjects()`. `CoverArt` уже умеет drag&drop и кнопку «Обложка».

**Приёмка:** перетащил картинку на карточку проекта → обложка сохранилась и показывается;
переживает перезапуск.

---

## ✅ T5 — Drag & Drop переупорядочивание глав в дереве (Medium) — СДЕЛАНО (commit a34290e)
> Все задачи T1–T5 выполнены и приняты. Дальше — «ФАЗА 2» ниже: крупные фичи делятся на
> фундамент (Codex) и финиш (senior). Начинай с **T6**.
### (архив описания T5)
**Зачем:** пожелания №7/№15 — управлять порядком глав.
**IPC уже готов:** `window.api.chapters.reorder({ projectId, storyId, order: string[] })` →
`Project` (`order` — массив id глав в новом порядке).

**Файл:** `src/renderer/src/app/Sidebar.tsx`. Использовать **уже установленный** `@hello-pangea/dnd`
(`DragDropContext`, `Droppable`, `Draggable`). Обернуть список глав внутри раскрытой истории.
На `onDragEnd`: посчитать новый порядок id и вызвать `chapters.reorder` → `applyProject`.

**Важно не сломать:** клик по главе (открыть), двойной клик, контекстное меню должны
продолжать работать. Перетаскивание — за «ручку» или за всю строку, но без конфликта с кликом.

**Приёмка:** главы переставляются мышкой, порядок сохраняется после перезапуска.

---

# 🚀 ФАЗА 2 — крупные фичи: модель «фундамент → финиш»

Дальше идут сложные интерактивные фичи (доска, таймлайн, шаблоны). Чтобы делить нагрузку,
каждая такая фича разбита на два слоя:

- **Часть Codex (фундамент)** — ограниченная и предсказуемая: модель данных, IPC, новая вкладка,
  CRUD и **статичный** рендер (без сложного интерактива). Это то, что ты делаешь.
- **Потом senior (финиш)** — сложный интерактив: бесконечный холст с зумом/панорамой,
  перетаскивание мышью, рисование стрелок, SVG-раскладки. Это делает Claude **поверх твоего
  фундамента**. Поэтому **строго соблюдай контракты типов из этого файла** — senior на них
  рассчитывает.

**Где оставлять швы для senior:** в местах, где нужен интерактив, ставь явный комментарий
`// SENIOR: <что тут будет>` и делай простую заглушку. Не пытайся реализовать зум/драг/рисование
стрелок — это не твоя часть, сделаешь лишнюю работу.

## Общие конвенции для новых фич
- **Новая вкладка:** расширь `OpenTab.kind` в `src/renderer/src/store/store.tsx` (как уже сделано
  для `'characters'`), добавь нужные поля (`boardId?`, `timelineId?`). В `src/renderer/src/app/App.tsx`
  добавь ветку рендера по `active.kind`. В `TabBar.tsx` (`addMenu`) добавь пункт открытия.
- **Новый раздел данных:** добавь типы в `src/shared/types.ts`, поле в `Project`, IPC в
  `src/main/ipc/index.ts` (через `mutate`), сигнатуры в `src/shared/api.ts`, проброс в `src/preload/index.ts`.
- **Автосохранение «тяжёлого» полотна** (доска): сохраняй весь объект целиком с дебаунсом ~600 мс,
  как сделано в редакторе (`src/renderer/src/features/editor/Editor.tsx`, `saveTimer`). Не дёргай
  IPC на каждое движение.
- CSS фичи — отдельный файл, импорт в `src/renderer/src/main.tsx`.

---

## ✅ T6 — Доска: данные + IPC + статичный рендер (Medium) — СДЕЛАНО (commit 8ab8c59, ревью senior пройдено)
> Фундамент принят. Финиш (S-A: зум/панорама, перетаскивание, рисование стрелок) делает senior
> поверх `features/board/Board.tsx`. Codex дальше может брать **T7** (независимая).
### (архив описания T6)
**Зачем:** объединённое пожелание №17 + №18 — безлимитная «доска детектива» как в Miro:
сколько угодно досок на проект, стикеры разных цветов/форм + стрелки между ними.
**Твоя часть — фундамент. Перетаскивание/зум/рисование стрелок НЕ делаешь (это senior).**

**1) Типы (`src/shared/types.ts`) — реализуй ровно так:**
```ts
export type StickerShape = 'rect' | 'rounded' | 'circle' | 'note'
export interface BoardSticker {
  id: string
  x: number; y: number   // координаты на холсте (мировые, не экранные)
  w: number; h: number
  color: string          // hex, напр. '#ffd166'
  shape: StickerShape
  text: string
}
export interface BoardArrow {
  id: string
  fromId: string         // id стикера-источника
  toId: string           // id стикера-цели
  color: string
  label?: string
}
export interface Board {
  id: string
  title: string
  stickers: BoardSticker[]
  arrows: BoardArrow[]
  createdAt: number
  updatedAt: number
}
```
И добавь в `Project` поле `boards: Board[]` (а также в `projects:create` инициализируй `boards: []`,
и в миграции `src/main/store/store.ts` — `boards: legacy.boards ?? []`).

**2) IPC (`src/main/ipc/index.ts` + `api.ts` + `preload`):**
- `boards:add({ projectId, title })` → `Project` (новый Board с пустыми массивами).
- `boards:rename({ projectId, boardId, title })` → `Project`.
- `boards:delete({ projectId, boardId })` → `Project`.
- `boards:save({ projectId, boardId, stickers, arrows })` → `Project` (полная перезапись полотна).

**3) Вкладка и открытие:** добавь `kind: 'board'` и `boardId?` в `OpenTab`; в `App.tsx` рендерь
`<Board boardId=... />`; в `TabBar.addMenu` — пункт «Новая доска» (создать + открыть) и список
существующих досок проекта для открытия.

**4) Компонент `src/renderer/src/features/board/Board.tsx` + `board.css` (СТАТИЧНЫЙ):**
- Контейнер-холст: `position: relative; overflow: hidden;`. Внутри — **обёртка трансформации**
  `<div className="board-world" style={{ transform: \`translate(${pan.x}px,${pan.y}px) scale(${zoom})\` }}>`
  с локальным состоянием `pan={x:0,y:0}`, `zoom=1`. **Пока не меняй их** — это шов для senior:
  `// SENIOR: pan & zoom (колесо/пробел-драг) меняют pan/zoom`.
- Стикеры: абсолютно позиционированные `div` по `x/y`, размер `w/h`, фон `color`, форма по `shape`
  (`circle` → border-radius 50%, `note` → лёгкий «загнутый угол» и т.п.). Текст — редактируемый
  (`textarea`/contentEditable), меняет `sticker.text`.
- На каждом стикере мини-панель: выбор цвета (`<input type=color>`), выбор формы (`<select>`),
  удаление. Кнопка «Добавить стикер» создаёт стикер в видимой области (напр. x=120+40*n, y=120).
- Стрелки: один **SVG-оверлей** на весь мир; для каждой `arrow` рисуй `<line>`/`<path>` от центра
  `fromId` к центру `toId` (центры считаются из `x+w/2`, `y+h/2`), цвет `arrow.color`, маркер-стрелку
  на конце (`<marker>`). Создание стрелки **мышью НЕ делай** — оставь
  `// SENIOR: рисование стрелок перетаскиванием от стикера к стикеру`. Для проверки можно временно
  добавить кнопку «связать выбранные», но это не обязательно.
- **Автосохранение:** локальный стейт `stickers`/`arrows`, при изменении — дебаунс-сейв через
  `boards:save` (см. конвенцию). Сидируй стейт из `board` при монтировании по `boardId`.

**Приёмка:** можно создать доску, добавить стикеры, поменять им текст/цвет/форму, удалить; стрелки
рендерятся между стикерами; всё сохраняется и переживает перезапуск; `npm run typecheck` чисто.
**Потом senior:** зум/панорама бесконечного холста, перетаскивание стикеров, рисование стрелок
мышью, разноцветные двусторонние связи + легенда, ресайз, выделение группой.

---

## T7 — Перенос главы между историями (Medium, целиком твоя)
**Зачем:** удобство (упоминалось в №7). Здесь senior-финиша нет — задача полностью твоя.

- **IPC:** `chapters:move({ projectId, fromStoryId, toStoryId, chapterId })` → `Project`. В `mutate`:
  убери главу из `fromStory.chapters`, добавь в конец `toStory.chapters`, пересчитай `order` в обеих
  историях, обнови `updatedAt`. Если `from===to` — ничего. Добавь в `api.ts` и `preload`.
- **UI (`src/renderer/src/app/Sidebar.tsx`):** в `chapterMenu` добавь пункт «Переместить» с
  `submenu` — список **других** историй проекта; клик вызывает `chapters.move` → `applyProject`.

**Приёмка:** глава переносится в выбранную историю, исчезает из старой, порядок корректный,
переживает перезапуск.

---

## ✅ T8 — Шаблоны анкет персонажей: фундамент (Medium) — СДЕЛАНО (commit cd94b26, ревью senior пройдено)
> Принято: CRUD шаблонов + применение к одному персонажу. Финиш (S-B: мультивыбор + применение
> к группе + распространение правок шаблона) — за senior. Codex дальше: **T9** (таймлайн).
### (архив описания T8)
**Зачем:** пожелание №16. **Твоя часть:** CRUD шаблонов + применение шаблона к **одному**
персонажу. Массовое применение к группе и распространение правок — senior.

- **Типы:** `CharacterTemplate { id, name, fieldLabels: string[] }`. Поле `Project.templates: CharacterTemplate[]`
  (init `[]` в create и миграции).
- **IPC:** `templates:add({projectId,name})`, `templates:update({projectId,templateId,patch})`
  (patch: `{name?, fieldLabels?}`), `templates:delete({projectId,templateId})`. Все → `Project`.
  Плюс в api/preload.
- **UI:** небольшой менеджер шаблонов (можно отдельной вкладкой `kind:'characters'` сверху, или
  модалкой «Шаблоны» из раздела персонажей): создать/переименовать/удалить шаблон; у шаблона —
  редактируемый список подписей полей (переиспользуй стиль из карточки персонажа).
- **Применить к одному:** на карточке персонажа — кнопка «Применить шаблон» (выбор из списка),
  которая **добавляет недостающие** поля (по `label`) в `character.fields`, не трогая существующие
  значения. Сохрани `character.templateId`.

**Приёмка:** можно завести шаблон с набором полей и применить его к персонажу — у него появляются
эти поля; переживает перезапуск. **Потом senior:** мультивыбор персонажей и применение к группе,
распространение правок шаблона на уже привязанных персонажей.

---

## ✅ T9 — Таймлайн: данные + IPC + список событий (Medium) — СДЕЛАНО (commit d858f2f, ревью senior пройдено)
> Принято: данные/IPC + линейный список событий. Финиш (S-C: раскладка «рыбья кость», ветки,
> драг/зум) — за senior. **Все задачи T1–T9 закрыты.** Дальше — только senior-финиши и чисто-senior.
### (архив описания T9)
**Зачем:** пожелание №2 (таймлайн, в идеале «рыбья кость»). **Твоя часть:** данные + простой
**линейный** список событий с CRUD. SVG-раскладку «рыбья кость» делает senior.

- **Типы:** `TimelineEvent { id, title, note: string, order: number }`,
  `Timeline { id, title, events: TimelineEvent[] }`. Поле `Project.timelines: Timeline[]` (init `[]`).
- **IPC:** `timelines:add`, `timelines:rename`, `timelines:delete`,
  `timelineEvents:add({projectId,timelineId,title})`, `timelineEvents:update`, `timelineEvents:delete`.
  Все → `Project`. Плюс api/preload.
- **Вкладка** `kind:'timeline'` + `timelineId?`; открытие из `TabBar`.
- **UI `features/timeline/Timeline.tsx`:** вертикальный/горизонтальный список событий с заголовком и
  заметкой, добавить/редактировать/удалить. Без перетаскивания и без SVG.

**Приёмка:** создаётся таймлайн, добавляются/редактируются/удаляются события, переживает перезапуск.
**Потом senior:** раскладка «рыбья кость» (хребет + наклонные «кости»), ветки/параллельные линии,
перетаскивание и зум.

---

# 🔒 Чисто senior (Codex не берёт совсем)
- Любые изменения в **редакторе и пагинации** (`src/renderer/src/features/editor/**`).
- Финишные интерактивные слои фич (см. статус ниже).

Если по ходу задачи ты упёрся во что-то из этого — стоп, отметь `// TODO(senior)` и опиши вопрос.

---

# ✅ Senior-финиши — статус (сделано Claude)
- **S-A — Доска:** зум/панорама, перетаскивание и ресайз стикеров, рисование стрелок,
  выбор/перекраска/подпись связей. (поверх T6)
- **S-B — Шаблоны:** мультивыбор персонажей, применение к группе, распространение правок
  шаблона на привязанных (IPC `characters:applyTemplate`). (поверх T8)
- **S-C — Таймлайн:** раскладка «рыбья кость» (SVG-хребет + наклонные кости) + переключатель
  Список/Рыбья кость. (поверх T9)
- **S-D — Редактор:** внутренние ссылки и подстраницы в стиле Notion (узел `InternalLink`,
  «Ссылка на главу» + «Создать подстраницу»). (№14)

**Все 19 пожеланий заказчицы закрыты.** Дальше — полировка по фидбеку (ФАЗА 3 ниже).

---

# 🔧 ФАЗА 3 — доработки по фидбэку заказчицы

Фидбэк (2026-06-16):
1. В редакторе **нет разрыва страниц как в Word** — лист непрерывный. Починить. → **S-E (senior)**.
2. На вкладке «Персонажи» непонятно, что делает **«Выбрать всех»**. → **T10 (Codex)**.
3. В Miro есть **другие элементы кроме стикеров** — добавить (текст, фигуры, картинки). → **T11 (Codex)**.
4. Хочется **привязывать к стикерам** инфо о персонажах/историях/таймлайнах — вики-ссылкой.
   → данные/чип **T12 (Codex)**, открытие + превью **S-F (senior)**.
5. В редакторе — **вики-ссылка** на имя/название с всплывающей справкой при наведении (как в
   Википедии). → **S-G (senior)**, расширение S-D.

## ✅ T10 — Персонажи: понятный режим выбора (Easy) — СДЕЛАНО (commit 14c3992)
**Проблема:** кликаешь «Выбрать всех» — визуально ничего не происходит (если нет шаблонов, панель
действий не появляется). Сделать понятнее.
**Файлы:** `src/renderer/src/features/characters/Characters.tsx`, `characters.css`.
- Панель выбора всегда показывает состояние: «Выбрано: N» (или «Ничего не выбрано»).
- Если шаблонов нет — показать подсказку: «Создайте шаблон во вкладке «Шаблоны», чтобы применять
  к группе». Кнопку «Применить к выбранным» делать **disabled** с понятным `title`, а не прятать.
- Подсветить выбранные карточки (класс `character-card--selected` уже есть) и показать, что выбор —
  это для массового применения шаблона. Можно добавить иконку/счётчик.
**Приёмка:** пользователю очевидно, зачем выбор и что делать после него; `npm run typecheck` чисто.

## ✅ T11 — Доска: новые элементы (текст, фигуры, картинка) (Medium) — СДЕЛАНО (commit ff9bd47)
**Зачем:** фидбэк №3. Сейчас на доске только стикеры-заметки. Добавить типы элементов как в Miro.
**Контракт типов (`src/shared/types.ts`) — расширить `BoardSticker`:**
```ts
export type StickerKind = 'note' | 'text' | 'shape' | 'image'
// добавить в BoardSticker:
//   kind: StickerKind        (по умолчанию 'note' — для старых данных подставляй 'note')
//   imagePath?: string       (для kind==='image', asset://-путь)
```
Старые данные без `kind` считай `note` (бэкфилл в `readProject`, как с `boards ??= []`:
для каждого стикера `sticker.kind ??= 'note'`).
- **text** — только текст, без фона/рамки (прозрачный); **shape** — фигура (используй существующие
  `shape: rect|rounded|circle` + при желании добавь `diamond`), цвет-заливка, текст по центру;
  **image** — картинка: загрузка через уже готовый `window.api.assets.saveImage({projectId,dataUrl})`
  (см. как в редакторе), `imagePath` рендерить через `asset://`.
- **UI:** в тулбаре доски вместо одной кнопки «Добавить стикер» — меню/группа: «Заметка», «Текст»,
  «Фигура», «Картинка». Рендер каждого типа в `Board.tsx` + стили в `board.css`.
- **Важно:** не ломай существующий интерактив (перетаскивание/ресайз/стрелки/зум) — он общий для
  всех элементов. Новые типы — это про внешний вид и `kind`, а не про новую механику.
**Приёмка:** можно добавить текст/фигуру/картинку, они двигаются/масштабируются/соединяются как
стикеры; старые доски открываются без потерь; `npm run typecheck` чисто.
**Потом senior (S-F):** ничего обязательного; при необходимости — полировка.

## ✅ T12 — Доска: привязка стикера к сущности проекта (Medium) — СДЕЛАНО (commit fd88b95)
**Зачем:** фидбэк №4. К стикеру можно прикрепить ссылку на персонажа/историю/таймлайн.
**Контракт типов — добавить в `BoardSticker`:**
```ts
//   link?: { kind: 'character' | 'story' | 'timeline'; id: string } | null
```
- **UI:** в мини-панели стикера — кнопка «Привязать» (иконка `Link2`), открывающая
  `openContextMenu` со списком сущностей проекта (персонажи `current.characters`, истории
  `current.stories`, таймлайны `current.timelines`). Выбор пишет `link` в стикер (через автосейв
  доски `boards:save`). На стикере с `link` рисуй **чип** с иконкой и названием сущности.
- **Клик по чипу пока НЕ обрабатывай** (открытие + превью — это senior). Оставь
  `// SENIOR: открыть привязанную сущность + превью-карточка` и data-атрибуты на чипе
  (`data-link-kind`, `data-link-id`), чтобы senior повесил поведение.
**Приёмка:** к стикеру привязывается сущность, чип отображается с её названием, сохраняется;
`npm run typecheck` чисто.
**Потом senior (S-F):** клик по чипу открывает вкладку сущности + всплывающая вики-карточка.

---

# ✅ Senior-финиши (Claude) — ФАЗА 3 (готово)
- **S-E — Разрывы страниц A4 как в Word.** ✅ commit 2a6a512. Непрозрачный зазор цвета стола
  + кромка листа → видимое разделение страниц. (ждёт визуальной проверки на длинном тексте)
- **S-F — Доска: открытие сущности + превью.** ✅ клик по чипу открывает сущность, наведение —
  вики-карточка.
- **S-G — Редактор: вики-ссылки.** ✅ марка WikiLink на выделенный текст → ссылка на любую
  сущность, клик открывает, наведение показывает карточку.

### (архив описания senior-очереди)
- **S-E — Разрывы страниц A4 как в Word (приоритет).** Сейчас лист непрерывный: фон бумаги задан
  на всём `.fc-prose`, поэтому межстраничные зазоры `tiptap-pagination-plus` сливаются. Починить:
  отдельные «листы» с зазором цвета стола + номера страниц; при необходимости — пересмотреть
  конфиг/верстку pagination-plus или заменить механизм. Зона `features/editor/**`.
- **S-F — Доска: открытие привязанной сущности + превью-карточка** (поверх T12): клик по чипу
  открывает вкладку; наведение — мини-карточка с инфо (вики-стиль).
- **S-G — Редактор: вики-ссылки** (расширение S-D `InternalLink`): ссылка на любую сущность
  (не только главу) + всплывающая справка при наведении.

---

# 🔧 ФАЗА 4 — доработки по фидбэку (раунд 3)

Фидбэк (2026-06-16):
1. Нет **удаления** созданных таймлайнов/досок. → **T13 (Codex)**.
2. **Авто-обновление ПО** при релизе (как в Telegram). → **S-H (senior, инфра)**.
3. **Экспорт «рыбьей кости» на доску** (картинкой), чтобы привязывать к стикерам. → **S-I (senior)**.
4. ~~Выгрузка историй в Telegram-бота~~ → **отложено** (решение: пока только авто-обновление).
5. Кнопка **«Помощь»** — ненавязчивый гид по приложению (подсветка элементов + подсказки,
   зачем функция). → **T14 (Codex)**.

Плюс стратегически: **миграция хранилища на SQLite** → **S-J (senior, отдельный рефакторинг)**.

## ✅ T13 — Удаление досок и таймлайнов (Easy) — СДЕЛАНО (commit a2c9097)
**Зачем:** фидбэк №1. IPC уже готов: `window.api.boards.delete({projectId,boardId})` и
`window.api.timelines.delete({projectId,timelineId})` (оба → `Project`). Нужен только UI.
**Файл:** `src/renderer/src/app/TabBar.tsx` (там уже есть списки «Доски проекта» и
«Таймлайны проекта» в меню «+», и доступны `current`, `applyProject`, `openTab`, `closeTab`).
- Каждый пункт доски/таймлайна сделай с `submenu`: «Открыть» и «Удалить» (`danger`).
- «Удалить» → `confirmDialog` → соответствующий `*.delete` → `applyProject(result)`. Если для
  этой доски/таймлайна открыта вкладка (`board:<id>` / `timeline:<id>`) — закрой её `closeTab`.
**Приёмка:** доску и таймлайн можно удалить из меню «+», открытая вкладка закрывается,
изменения переживают перезапуск; `npm run typecheck` чисто.

## ✅ T14 — Кнопка «Помощь»: ненавязчивый гид (Medium) — СДЕЛАНО (senior-полировка: якорь editor-toolbar)
**Зачем:** фидбэк №5. Кнопка «?» запускает пошаговый тур: подсвечивает элемент интерфейса и
показывает подсказку, что это и зачем.
**Файлы:** новый `src/renderer/src/features/help/HelpTour.tsx` + `help.css`; импорт css в
`src/renderer/src/main.tsx`; кнопка-триггер «?» в `TabBar.tsx` (рядом с темами) и на `Home`.
- **Подсветка:** оверлей затемняет экран, вырез вокруг целевого элемента (приём: див по
  `getBoundingClientRect` цели с огромным `box-shadow: 0 0 0 9999px rgba(0,0,0,.6)`), рядом —
  тултип с заголовком/текстом и кнопками «Далее» / «Пропустить» / «Назад».
- **Шаги** — массив `{ selector, title, text }`. Навешивай на ключевые элементы атрибуты
  `data-tour="library|tree|add-tab|editor-toolbar|theme"` (sidebar, кнопка «+», тулбар редактора,
  переключатель тем и т.п.) и ссылайся на них по селектору. Если элемента нет на экране — пропускай шаг.
- Тур запускается по кнопке «?». Можно (необязательно) показывать при первом запуске через
  `localStorage` флаг `fancreator.tour.seen`.
- Текст шагов — по-русски, дружелюбно и кратко.
**Приёмка:** «?» запускает тур, подсветка попадает по элементам, шаги листаются, «Пропустить»
закрывает; не ломает клики (оверлей поверх, но тур-режим — модальный); `npm run typecheck` чисто.
**Senior-финиша не требует.**

---

# ▶️ Senior-очередь — ФАЗА 4 (Claude)
- **S-H — Авто-обновление** ✅ commit (electron-updater + publish github + UpdateBanner).
  Реально заработает после первого релиза с тегом на GitHub Releases.
- **S-I — Экспорт «рыбьей кости» на доску** ✅ commit (кнопка «На доску» в режиме рыбьей кости).
- **S-J — Миграция на SQLite** ✅ commit. `db.ts` (документ в строке + FTS5), `store.ts` —
  фасад с фолбэком на JSON при сбое `initDb`, НЕразрушающая миграция из `project.json`
  (json остаётся бэкапом), поиск глав через FTS5. Native `better-sqlite3` (N-API prebuilt).
  ⚠️ Нужна визуальная проверка в собранном/dev окне: что БД поднялась (нет варнинга
  «SQLite недоступен» в терминале) и старые проекты на месте. (`better-sqlite3`): таблицы (projects, stories, chapters,
  characters, character_fields, templates, boards, board_stickers, board_arrows, timelines,
  timeline_events, sticker_links); посохранение по сущностям вместо перезаписи всего проекта;
  FTS5 по `chapters.plainText` для быстрого поиска; одноразовая миграция из `project.json`.
  Отдельный рефакторинг store+IPC, делать аккуратно с бэкапом данных.

---

# 🎨 ФАЗА 5 — редизайн библиотеки под Apple Books

## ✅ T15 — Обложки в стиле iBooks + перелистывание (Medium) — СДЕЛАНО (commit a14cc96; senior-полировка: z-index при наведении)
**Зачем:** заказчица хочет, чтобы папки/полка выглядели как **Apple Books**, а анимацию
«виниловой пластинки» при наведении заменить на **перелистывание страниц**.

**Главный файл:** `src/renderer/src/features/library/CoverArt.tsx` и
`src/renderer/src/features/library/library.css`. Компонент используется и для проектов
(`Home.tsx`), и для историй (`Shelf.tsx`) — **не меняй его props** (`title`, `coverPath`,
`onClick?`, `onDropImage?`, `onPick?`) и места использования.

**Что сделать:**
1. **Убрать винил.** Удалить элемент `.cover-disc` из разметки и все связанные стили
   (`.cover-disc`, `.cover-disc::after`, `@keyframes spin`, `.cover:hover .cover-disc`,
   `.cover:hover .cover-sleeve`).
2. **Книга в стиле Apple Books** (модерн, чистый, без деревянных полок):
   - 3D-обложка: контейнер с `perspective`, у книги — **корешок** слева (тёмная узкая
     грань / внутренняя тень от левого края), **срез страниц** справа (тонкие светлые
     линии у правой кромки), скругление углов справа, мягкая «приподнятая» тень снизу.
   - Соотношение сторон обложки ~ **2:3**, фон-заглушка (если нет `coverPath`) — как сейчас
     `.cover-fallback`, но в книжном стиле (корешок + название серифом).
   - Лёгкий **hover-lift** (приподнять + усилить тень).
3. **Анимация перелистывания** вместо вращения винила:
   - При наведении (и/или клике-открытии) передняя обложка **раскрывается как книга** —
     поворот вокруг левого ребра: `transform: rotateY(...)` с `transform-origin: left center`,
     под ней проглядывает «страница». Сделай плавно (`transition`/`@keyframes`, ease из
     `--ease`). Допустим тонкий «переворот страницы» (элемент-страница, отгибающийся справа).
   - Анимация должна читаться как «листаем книгу», быть аккуратной и не дёрганой.
4. Сохранить **drag&drop** обложки (рамка `drag-over`), кнопку `onPick` («Обложка») и `onClick`.
5. Цвета — только через CSS-переменные; вид консистентен во всех 6 темах.

**Приёмка:** проекты и истории выглядят книгами как в Apple Books; при наведении —
перелистывание/раскрытие (винила больше нет); drag&drop обложки и кнопка работают;
`npm run typecheck` чисто; ничего не сломалось в `Home`/`Shelf`.
**Потом senior (S-K):** полировка тайминга/3D-ощущения анимации при ревью.

---

# 🧩 ФАЗА 6 — правки по фидбэку заказчицы (раунд 4)

Полный список — 32 пункта (PDF «заметки для FanCreator»). Ниже разбито: что делает Codex,
что — senior. **Редактор и пагинация — зона senior** (там же критичные баги).

## Задачи Codex

### ✅ T16 — Доска: чистка и удобство — СДЕЛАНО (commit 24e252e)
Файлы: `src/renderer/src/features/board/Board.tsx`, `board.css`.
- **п.10** — инструменты стикера (цвет/форма/удалить/привязать) спрятать в меню «⋯»
  (кнопка в углу стикера → `openContextMenu` со всеми действиями), чтобы не загромождать стикер.
- **п.19** — действие «Дублировать» элемент (стикер/текст/фигура/картинка): копия рядом
  с тем же цветом/размером/типом, новый `id`.
- **п.20 (баг)** — при перетаскивании стикера выделяется весь текст на доске. Починить:
  на время drag вешать `user-select: none` (на `.board` или body) и/или `e.preventDefault()`
  в `pointerdown`.
- **п.24** — для `kind:'image'` убрать фон/рамку «стикера» (картинка без подложки), оставить
  только изображение + ручки.
- **п.21** — «починить текст в доске»: проверить редактирование текста в `text`/`shape`
  (фокус, перенос строк, что drag не мешает печатать).
**Приёмка:** меню «⋯» работает, дублирование создаёт копию, перетаскивание не выделяет текст,
картинки без подложки, текст редактируется; `npm run typecheck` чисто.

### ✅ T17 — Панель вкладок: прокрутка (Easy, п.29) — СДЕЛАНО (commit 9edbbef)
Файлы: `src/renderer/src/app/TabBar.tsx`, `app.css`.
При открытии многих вкладок их ряд «выдавливает» переключатель тем/«+»/«?» вбок.
Сделать: ряд вкладок — отдельный контейнер с `overflow-x: auto` (горизонтальный скролл),
а кнопки «+», «Помощь» и `ThemeSwitcher` — зафиксированы и не уезжают.
**Приёмка:** при 10+ вкладках элементы справа на месте, вкладки скроллятся; typecheck чисто.

## Senior-очередь (Claude) — редактор и данные
- **S-L (частично ✅: перф больших глав, чистка вставки):** буфер обмена и стабильность —
  п.1 (виснет при вставке большого текста), п.2 (теряется форматирование, лишние пустые
  строки), п.14 (копирование из приложения наружу ломается), п.7 (undo/redo: «отменить»
  один раз, «повторить» не работает), п.23 (вставка картинки не разбивает страницы).
- **S-M:** списки и форматирование — п.9 (автонумерация не отключается обычным текстом),
  п.25 (размер номера ≠ текста), п.26 (двойной клик сбрасывает размер на 16; 16 после 36
  ломает нумерацию), п.28 (подчёркивание не охватывает у/У/Д/д), п.8 (пробел в «1 стр.»).
- **S-N ✅ (шрифты, размеры, № без «Страница», межстрочный, палитра):** тулбар — п.5 (шрифты: добавить Arial и др., больше размеров), п.27 (палитра цветов
  + кружок выбора), п.31 (межстрочный интервал и доп. форматирование), п.6 (нумерация
  страниц: убрать слово «страница», варианты расположения номера).
- **S-O ✅ проверка орфографии** — п.4 (Electron spellchecker RU/EN, подчёркивание ошибок).
- **S-P:** оглавление по главе — п.3 (TOC из заголовков с переходом; многофайловость сохранить).
- **S-Q:** картинки в редакторе — п.15 (улетают при вставке/перемещении), п.24 (масштабирование
  картинок в тексте).
- **S-R:** корзина — п.30 (мягкое удаление + восстановление; затрагивает SQLite-хранилище).
- **Отложено/обсудить:** п.17 (текст вдоль кривой линии) — сложно, вынести в отдельную фичу.

---

# 🧩 ФАЗА 7 — большой фидбэк (раунд 5), 13 пунктов

Делим: Codex — доска + каркасы новых структур (подглавы, под-события, иерархия);
senior — редактор и архитектура папок + финишные раскладки. **Редактор/пагинация — senior.**

## Задачи Codex

### T18 — Доска: два фикса
- **#6** Маленький стикер поверх большого нельзя перетащить. Причина — порядок отрисовки/
  перекрытие. Почини: при `pointerdown` на стикере поднимать его наверх (перемещать в конец
  массива `stickers` = выше по z-order) перед началом перетаскивания; убедиться, что drag
  берёт именно тот элемент, по которому кликнули.
- **#8** Подпись связи (`arrow.label`) плохо лежит — повернуть текст **вдоль линии** (тот же
  угол). В SVG: вычислить угол `atan2(y2-y1, x2-x1)` и применить `transform="rotate(deg x y)"`
  к `<text>` подписи (в градусах, около середины линии). Длинные горизонтально-перевёрнутые
  углы (>90°/<-90°) развернуть на 180°, чтобы текст не был вверх ногами.
**Приёмка:** маленький стикер двигается поверх большого; подписи связей идут вдоль линий; typecheck чисто.

### T19 — Подглавы (#11)
**Контракт:** добавь в `Chapter` поле `parentId?: string | null` (родительская глава внутри
той же истории; null/undefined — глава верхнего уровня).
- IPC: `chapters:add` принять необязательный `parentId`; новый `chapters:setParent({projectId,
  storyId,chapterId,parentId})`. (через `mutate`, + api/preload).
- Sidebar: показывать главы **деревом** (подглавы с отступом под родителем, раскрытие).
  В контекстном меню главы — «Добавить подглаву». Учитывай корзину (`deletedAt`) и dnd
  (переупорядочивание — в пределах одного уровня; если сложно, временно отключи dnd для
  вложенных и оставь `// TODO(senior)`).
**Приёмка:** можно создать подглаву, она вложена в дереве, открывается в редакторе, переживает
перезапуск.

### T20 — Под-события схемы Исикавы (#12)
**Контракт:** добавь в `TimelineEvent` поле `parentId?: string | null`.
- IPC: `timelineEvents:add` принять `parentId?`; событие может иметь под-события.
- В режиме **Список** таймлайна показывай под-события с отступом + «Добавить под-событие».
- В режиме «рыбья кость» рендер под-костей **НЕ** делай — оставь `// SENIOR: под-кости на схеме`.
**Приёмка:** под-события создаются и видны в списке; данные сохраняются; typecheck чисто.

### T22 — Иерархическая структура с ориентацией (#13)
**Зачем:** заказчица хочет диаграмму-иерархию (как орг-схема) с выбором направления
(вертикально/горизонтально). Делаем как доску/таймлайн.
**Контракт (types.ts):**
```ts
export interface HierarchyNode { id: string; parentId: string | null; title: string }
export interface Hierarchy {
  id: string; title: string; orientation: 'vertical' | 'horizontal'; nodes: HierarchyNode[]
}
// Project.hierarchies: Hierarchy[]  (+ init [] в create и миграции, backfill в readProject)
```
- IPC: `hierarchies:add/rename/delete`, `hierarchyNodes:add({...,parentId})`, `…:update`, `…:delete`.
- Вкладка `kind:'hierarchy'` + `hierarchyId?`; открытие из меню «+».
- UI `features/hierarchy/Hierarchy.tsx`: переключатель ориентации; **простой** рендер дерева
  (вложенные блоки с отступами) + CRUD узлов. **Красивую авто-раскладку с коннекторами
  (как на скрине орг-схемы) НЕ делай** — оставь `// SENIOR: авто-раскладка + соединители`.
**Приёмка:** создаётся иерархия, узлы/подузлы добавляются, ориентация переключается, сохраняется.

## Senior-очередь (Claude)
- **S-1** Поиск с подсветкой совпадений (#1) — decoration-плагин, подсветить все совпадения.
- **S-2** Tab не уводит фокус (#2) — Tab в редакторе всегда «съедается» (не перескакивает по элементам).
- **S-3** Умная вставка (#3) — сохранять шрифт и размер при вставке из Word (textStyle font-family/size).
- **S-4** ✅ Свободное перемещение картинок (#4) — режим «перед текстом»: position:absolute, drag мышью, координаты x/y в атрибутах ноды (ResizableImage).
- **S-5** ✅ DELETE как в Word (#5) — Ctrl+Delete/Ctrl+Backspace удаляют слово (WordDelete); базовые Delete/выделение/слияние абзацев/конец документа — штатный keymap ProseMirror.
- **S-6** Экспорт Word (#7) — с картинками, шрифтами, отступами, интервалом (не плоский текст).
- **S-7** Масштаб страницы (#9) — зум листа в редакторе.
- **S-8** Папки/подпапки в библиотеке (#10) — модель данных + IPC (из-за SQLite — senior), UI потом.
- **S-9** ✅ Под-кости на схеме Исикавы (#12) — дочерние события рисуются под-костями вдоль главной кости (и на экране, и в экспорте на доску); авто-раскладка иерархии + соединители (#13) — CSS-оргсхема: вертикали/шины между родителем и детьми (горизонтальная ориентация), засечки в вертикальной.

## Дополнения к ФАЗЕ 7 (раунд 5.1)

### T23 — Перетаскивание для перестановки (Codex)
Заказчица хочет менять порядок drag&drop (используем уже установленный `@hello-pangea/dnd`).
- **Сайдбар:** перетаскивание **историй** местами (нужен новый IPC `stories:reorder({projectId,
  order: string[]})` — по образцу `chapters:reorder`); **глав** и **подглав** (внутри своего
  уровня/родителя). У глав reorder уже есть — расширь на подглавы (порядок среди детей одного
  parentId; учитывай `deletedAt`).
- **Таймлайн (список):** перетаскивание **событий** и **под-событий** (порядок среди одного
  уровня). Нужен IPC `timelineEvents:reorder` или сохранение нового `order` через update.
- **Иерархия (T22):** перестановка узлов среди соседей (если T22 ещё в работе — сделать после неё).
**Приёмка:** порядок меняется мышью во всех трёх местах, сохраняется после перезапуска; не ломает
клик/контекстные меню; typecheck чисто.

### S-10 — Tab как в Word (senior)
Сейчас Tab всегда увеличивает красную строку. Сделать по-Word: Tab в начале абзаца — красная
строка; Tab в середине — табуляция (символ \t); Shift+Tab — обратно. (делает Claude)

---

# 🎨 ФАЗА 8 — Редизайн под уровень Linear / Raycast / Notion

Контекст: заказчица хочет «больше удобства и комфорта», Codex прислал разбор (depth, типографика,
motion, command palette, bento, dashboard). Сеньор отфильтровал. **Жёсткие правила фазы:**
- **Обложки-книги НЕ убираем** — заказчица их явно просила (Apple Books). Карточки уплотняем,
  но обложка остаётся. Никакого bento «вместо книг».
- **Сайдбар она любит** — только аддитивные правки, ничего не ломаем.
- **Без GSAP/Motion** — только CSS-transitions (не тащим либы в Electron).
- **Поиск отдельным дропдауном не делаем** — он вливается в command palette (Ctrl+K).
- **Темы:** сперва прототип токенов на тёмной теме (ревью), потом порт на остальные 5.
- Дизайн-токены — в `src/renderer/src/theme/theme.css`; правки компонентов — в их CSS-файлах.

## Задачи Codex (фундамент)

### T-D1 — Типографика и шкала (Easy/Medium)
- Подключить **Inter** для UI (через `@fontsource/inter` или локальный шрифт; согласовать с senior,
  как уже подключены шрифты редактора). Заголовочный/serif для контента не трогаем.
- Ввести CSS-переменные шкалы в `theme.css`: `--fs-display, --fs-h1, --fs-h2, --fs-title, --fs-body,
  --fs-meta, --fs-caption` и веса `--fw-regular/medium/semibold/bold`.
- Применить к существующим заголовкам/мете (`.home-title`, `.library-section h2`, `.book-title`,
  `.book-meta`, `.library-story-*`, `.characters-head` и т.п.). Цель — внятная иерархия.
**Приёмка:** единообразная типографика, заголовки крупнее/контрастнее меты; typecheck чисто.

### T-D2 — Микроанимации на CSS (Easy)
- Карточки (`.project-card`, `.library-story-card`, `.library-folder-card`, `.library-material-card`,
  `.book`): hover — подъём ~4px + подсветка бордера/мягкая тень (плавный `transition`).
- Строки сайдбара и кнопки: лёгкий hover-фон/мягкое свечение акцентом. **Только** CSS.
- Уважать `@media (prefers-reduced-motion: reduce)` — гасить анимации.
**Приёмка:** интерфейс «живой», без рывков; ничего не дёргается при загрузке.

### T-D3 — Glassmorphism на оверлеях (Easy)
- Только оверлеи: `.modal`/`.modal-overlay`, контекстное меню (`.ctx-menu`), палитра цветов
  (`.color-palette-panel`), плавающие панели. `backdrop-filter: blur(16px)` + полупрозрачный фон.
- Основной фон/карточки **не** трогать. Проверить читаемость на всех 6 темах.
**Приёмка:** оверлеи выглядят «стеклянными», текст читается.

### T-D4 — Карточки историй: плотнее + bento-мета (Medium)
- Оставить обложку-книгу. Рядом — компактный bento-блок меты: главы, персонажи, слова,
  «изменено N назад» (данные уже есть: `chapters`, `wordCount`, `updatedAt`; персонажей берём из
  проекта). Меньше пустоты, аккуратная сетка.
- Не ломать существующий `.library-story-card` грид; согласовать с S-D1 (токены) если уже влиты.
**Приёмка:** карточка плотная и информативная, обложка на месте.

### T-D5 — Порт палитры на остальные темы (Medium) — ПОСЛЕ S-D1
- Когда senior утвердит токены слоистых поверхностей на тёмной теме — повторить 4-уровневую
  палитру + акцент/бордеры для остальных 5 тем в `theme.css`, сохранив их характер.
**Приёмка:** все темы получили глубину, ничего не «слиплось».

## Senior-очередь (Claude)

- **S-D1** ✅ Дизайн-токены: 4 уровня поверхностей `--surface-0..3` + акцент `#a78bfa` (+hover/glow) +
  тонкие бордеры + `--elev-1..3`/`--ring`/`:focus-visible`. Прототип на тёмной теме, фоллбэки в
  `:root`. (`a4a73b9`)
- **S-D2** ✅ Command palette (Ctrl/⌘+K): фаззи-поиск, создать историю/папку/персонажа/таймлайн/
  доску, найти главу, недавнее, переход; на главном — открыть проект. Вобрал поиск. (`0a96a31`)
- **S-D3** ✅ Дашборд «Continue writing» в библиотеке: карточка «продолжить последнюю главу» +
  сводка (слова/% готово/главы) + 3 недавние. Без бэкенда. (`e6bf5d7`)
  - follow-up (опц.): «слов сегодня» + streak требуют дневного лога (бэкенд) — отдельной задачей.
- **S-D4** ✅ Сайдбар: блок «Разделы проекта» под деревом историй — Персонажи (переход) +
  сворачиваемые Таймлайны/Доски (открытие вкладок). Дерево и DnD не тронуты. (`63caaf8`)

**ФАЗА 8 — senior-часть закрыта.** Локации в сайдбаре не выносил (нельзя deep-link на папку
персонажей — единая вкладка с внутренним состоянием); при желании добавим переход на вкладку
Персонажи с пред-выбором папки отдельной задачей.

---

# ✨ ФАЗА 9 — полировка и удобство (после редизайна)

Контекст: фидбэк заказчицы + идеи из Wattpad. Правила прежние: обложки-книги и
дерево сайдбара не ломаем; без тяжёлых либ; CSS — в файлах компонентов; данные —
типы→IPC→api→preload→normalizeProject.

## Задачи Codex

### T-E1 — Чип-подсказка «⌘K» в поиске сайдбара (Easy)
- В строку поиска проекта (сайдбар) добавить ненавязчивый бейдж `Ctrl K` справа.
- По клику открывать палитру: `import { openCommandPalette } from '../shared/ui/CommandPalette'`
  (опенер уже есть) → `onClick={openCommandPalette}`. Не ломать сам поиск.
**Приёмка:** бейдж виден, клик открывает Ctrl+K-палитру; ввод в поиск работает как раньше.

### T-E2 — Фильтр по жанрам/тегам на полке (Medium, идея Wattpad)
- На главной библиотеке (Shelf), рядом с поиском/сортировкой — строка чипов жанров/тегов,
  собранных из `story.genres`/`story.tags` активных историй. Клик по чипу фильтрует
  `visibleStories` (мультивыбор — И/ИЛИ на ваш вкус, по умолчанию ИЛИ). Снять — повторный клик.
- Не дублировать логику поиска; фильтр применяется поверх (комбинируется с query).
**Приёмка:** чипы появляются при наличии тегов/жанров; фильтрация корректна; сброс работает.

### T-E3 — Оценка времени чтения + статус истории (Easy/Medium)
- В карточке истории и в шапке полки показывать **время чтения** (≈ слова / 200, округлить,
  формат «~7 мин»); helper рядом с `compactNumber`. Использовать существующий `story.status`
  (бейдж уже есть) — убедиться, что отображается единообразно.
- Возрастной рейтинг (18+ и т.п.) **не делать** — поле добавит senior (S-E3), тогда отобразите.
**Приёмка:** время чтения корректно для главы/истории/проекта; не ломает карточку.

### T-E4 — Единые пустые состояния (Easy)
- Привести к одному стилю пустые экраны: Home (нет проектов), Shelf (нет историй/в папке),
  Characters (нет персонажей/в папке), Board (пустая доска), Timeline (нет событий).
- Иконка + заголовок + подсказка + первичная кнопка действия; использовать токены S-D1
  (`--surface-*`, мягкая рамка). Сверстать переиспользуемый класс `.empty-state` в ui.css.
**Приёмка:** все пустые состояния выглядят одинаково «дорого» и зовут к действию.

### T-E5 — Светлые темы и контраст (Easy)
- Проверить читаемость на бумажных/светлых акцентах: glass-оверлеи (палитра, модалки,
  command palette), бейджи, `--text-faint` на светлом. Поправить контраст там, где слабо.
- Проверить, что слоистые `--surface-*` (из T-D5) дают глубину и на не-тёмных темах.
**Приёмка:** на всех 6 темах текст читается, оверлеи не «молочные».

## Senior-очередь (Claude) — ФАЗА 9

- **S-E1** Индикатор автосохранения в редакторе («Сохранено · чч:мм» / «Сохранение…») в статус-баре.
- **S-E2** Режим чтения/предпросмотр главы (чистая типографика, без тулбара, темы, время чтения).
- **S-E3** Возрастной рейтинг истории (поле модели + IPC + свойства) — затем Codex отобразит (T-E3).
- **S-E4** Дневной лог письма → «слов сегодня» + серия дней (streak) в дашборде S-D3 (бэкенд-лог).
- (позже) EPUB-экспорт готовой истории.

---

# 🧩 ФАЗА 10 — фидбэк заказчицы по v2.0.4

Источник: «заметки (1).docx». Правила прежние (обложки-книги и дерево не ломаем; без новых
либ; данные типы→IPC→api→preload→normalize; русский UI; typecheck чисто).

## Задачи Codex (UI/механика)

### T-F1 — Шрифты заголовков и узкий селект сортировки (Easy)
- Заголовки экранов «Истории»/«Персонажи» разного размера, хотя оба «заголовок 1». Унифицировать:
  у Characters шапка `.home-title` со `style={{fontSize:24}}` — убрать инлайн, привести к единому
  стилю как на полке (через `--fs-h1`/класс). Проверить и Timeline/Hierarchy-нет/Board.
- Селект «Сначала недавние» (`.library-sort`) не вмещает слово — расширить ширину/`min-width`.
**Приёмка:** заголовки одинаковые на всех экранах; текст в селекте помещается.

### T-F2 — Читаемый текст на светлой обложке (Easy)
- Если у истории выбран светлый `color`, белый текст названия на заглушке-обложке не виден.
  Считать контрастный цвет (яркость `--cover-color`: тёмный текст на светлом, белый на тёмном) —
  helper в `CoverArt.tsx`/CSS. Применить к `.cover-fallback-title`.
**Приёмка:** название читается на любом цвете обложки.

### T-F3 — Пустое имя нового персонажа (Easy)
- Новый персонаж создаётся с именем-значением «Новый персонаж». Сделать имя пустым, а текст —
  плейсхолдером (как у «Роль»). Поправить дефолт в `characters:add` (main) на `''` и плейсхолдер
  в поле имени. (Аналогично фиксу пустого текст-стикера на доске.)
**Приёмка:** у нового персонажа имя пустое с серой подсказкой, стирать ничего не нужно.

### T-F4 — Убрать «локации» из названий папок персонажей (Easy)
- В разделе «Персонажи» папки названы «Папки / локации», диалоги «Новая папка / локация»,
  панель «LocationPanel», подписи «Описание локации»/«Концепт-арты локации». Заказчица: папка —
  не только локация. Переименовать в нейтральное: «Папки», «Новая папка», «Описание», «Изображения».
  Описание и картинки папки оставить.
**Приёмка:** в UI нет слова «локация»; функциональность папок не изменилась.

### T-F5 — «+» сверху: «Новый персонаж» вместо «Персонажи» (Easy)
- В меню «+» (`TabBar.addMenu`) пункт «Персонажи» сейчас просто открывает экран. Сделать
  «Новый персонаж»: создаёт персонажа (`characters.add`) и открывает вкладку «Персонажи».
  (Выбор папки при создании — общий механизм из S-F10; пока без папки.)
**Приёмка:** пункт создаёт персонажа, как остальные пункты создают сущности.

### T-F6 — Папки по умолчанию свёрнуты (Easy)
- Сейчас при открытии проекта папки раскрыты (`expanded[key] ?? true`). Сделать по умолчанию
  свёрнутыми (`?? false`) в сайдбаре, на полке и в разделе персонажей.
**Приёмка:** после открытия проекта папки закрыты, раскрываются по клику.

### T-F7 — «Все истории» на полке — сворачиваемый раздел (Easy/Medium)
- Заголовок секции «Все истории» сделать кликабельным (свернуть/развернуть список), чтобы быстро
  проскроллить к материалам. Состояние — локально в компоненте.
**Приёмка:** секция сворачивается/разворачивается, не ломая фильтры/поиск.

### T-F8 — Просмотр набросков (lightbox) (Medium)
- Картинки в `ImageStrip` сейчас только миниатюры. По клику — открывать полноэкранный просмотр
  (оверлей с затемнением, картинка по центру, Esc/клик-вне закрывают; стрелки между картинками —
  опционально). Переиспользовать на персонажах и в папках.
**Приёмка:** клик по миниатюре открывает крупный просмотр; Esc закрывает.

### T-F9 — Сворачиваемое описание полей персонажа (Easy/Medium)
- У полей анкеты длинные описания. Дать сворачивать/разворачивать значение поля (или все поля).
**Приёмка:** описания можно скрыть, карточка компактнее.

### T-F10 — Перетаскивание верхних вкладок (Medium)
- В `TabBar` разрешить менять порядок открытых вкладок мышью (массив `tabs` в сторе; добавить
  `reorderTabs(from,to)` или через `@hello-pangea/dnd`). Вкладку «Библиотека» можно зафиксировать
  первой. Не ломать закрытие/активацию.
**Приёмка:** вкладки переставляются мышью, порядок держится в рамках сессии.

### T-F11 — Быстрый последний цвет в палитре (Medium)
- В `ColorPalette` добавить поведение «как в Word»: основной клик применяет последний выбранный
  цвет, отдельная стрелка/зона открывает палитру выбора. Хранить последний цвет (на инстанс/гло­
  бально). Применить к маркеру выделения и цвету текста в редакторе.
**Приёмка:** повторное выделение цветом — один клик, без выбора каждый раз.

### T-F12 — Замена шаблона анкеты (Medium)
- При применении шаблона к персонажу, у которого уже есть поля, спросить `confirmDialog`:
  «Заменить анкету шаблоном «X»? Отмена — добавить недостающие поля». ОК = заменить поля шаблоном
  (сохранив значения совпадающих по label), Отмена = текущее поведение (добавить недостающие).
**Приёмка:** можно заменить ошибочно набранную анкету, не удаляя поля вручную.

## Senior-очередь (Claude) — ФАЗА 10

- **S-F1** ✅ Регресс drag-reorder: причина — `backdrop-filter: blur` на `.sidebar` (containing
  block ломал @hello-pangea/dnd). Фон сайдбара сделан сплошным `--surface-1`. (`9a38cfe`)
- **S-F2** ✅ Переключение глав: убран `autofocus:'end'` (прыжок вниз/лишняя строка). Контрольная
  точка прокрутки на главу (localStorage) + открытие истории на последней главе. (`609dc25`)
- **S-F3** ✅ Редактор: умная вставка v2 — снимаем чужие font-size/family/line-height и `<o:p>`,
  убираем пустые абзацы; текст принимает стиль документа. (`b89afe9`)
- **S-F4** ✅ Редактор: Backspace в начале пункта списка выводит пункт из списка (как Word),
  а не клеит к предыдущему; Ctrl+Backspace по словам — WordDelete. (`39cddf3`)
- **S-F5** ✅ Формат по образцу + красная строка (ч.1, `7e6993f`); ручной разрыв страницы (ч.2,
  `62b08a8`) — узел-спейсер `pageBreak`, заполняет до границы страницы пагинатора. Эксперим.,
  проверить вживую.
- **S-F6** Импорт Word: расхождение числа страниц, размера шрифта и полей листа (обсудить детали).
- **S-F7** Живое обновление сайдбара/счётчиков при правке главы из правого блока «Continue writing»
  (сейчас только после перезапуска). Возможно общий корень с «возврат на стартовый экран».
- **S-F8** ✅ Персонажи в сайдбаре — сворачиваемое дерево по папкам (рекурсивно) + персонажи;
  клик открывает страницу персонажа, «→» открывает раздел. (`e6c4587`)
- **S-F9** ✅ (v1, `fc58369`) Папка → компактная карточка персонажа (аватар 3×4 + имя/роль/цвет) →
  отдельная вкладка `character` со страницей: аватар, имя/роль/папка/цвет/теги, анкета (поля +
  применение шаблона с выбором «заменить/дополнить» — **закрывает T-F12, на инлайн-карточке его
  делать НЕ нужно**), галерея артов. Осталось: удалить мёртвый инлайновый `CharacterCard` из
  `Characters.tsx` (senior, отдельным коммитом); опц. кадрирование аватара.
  ⛔ Codex: `Characters.tsx` сейчас активно меняется senior — по разделу персонажей согласуй.
  <details><summary>исходный план</summary>
  модель **папка → карточка персонажа (как обложка истории) → отдельная страница персонажа**.
  - Раздел персонажей становится как библиотека: папки + сетка карточек персонажей (с аватаром),
    клик открывает **индивидуальную страницу персонажа** (отдельная вкладка/вид).
  - Страница персонажа — вики-стиль + конструктор как в D&D (референс
    longstoryshort.app/characters/builder): аватар (фото 3×4 сверху по центру), имя/роль,
    блоки-характеристики (анкета/шаблоны), — **оставляем только то, что нужно для персонажа**.
  - На странице — **коллаж иллюстраций/артов**, каждую можно открыть полноценно по двойному клику.
  - Аватар: `avatarPath` (уже в модели) добавить в patch + загрузка/кадрирование; цвет карточки.
  - Этап 1 (senior-план): модель/IPC (avatarPath в patch, цвет персонажа, вкладка `character`),
    карточная сетка + страница. Мелочи (lightbox, сворачивание) могут лечь на Codex (T-F8/T-F9).
  </details>
- **S-F10** ✅ Диалог `promptStory` (название + выбор папки) при создании истории из «+» и
  command palette. (`5df1f77`)
- **S-F11** ✅ Сайдбар: цвет истории/папки — в ПКМ-меню (подменю «Цвет»); вместо постоянной палитры
  у папок кнопка «→ перейти в папку» (+пункт меню), открывает библиотеку на нужной папке. (`78b34f2`)

## Решено (бывшее «обсудить»)
- ✅ Удаление папки — **вместе с содержимым** (истории → Корзина, персонажи безвозвратно). Сделано
  (`36f4bb7`).
- ✅ «Формат по образцу» — как в Word (см. S-F5).
- ✅ Аватар/страница персонажа — полноценная вики-страница (см. S-F9), не просто flip-card.
- % готовности = главы «Готово» ÷ всего глав (вынести в тултип — Codex, мелочь).
- Лишние строки при копировании: источник чистый (`<p>`), проблема в HTML из буфера (Word/Docs:
  пустые абзацы + размеры в pt). Чинить в `transformPastedHTML` с живой проверкой буфера (S-F3).

---

# 🧩 ФАЗА 11 — фидбэк по v2.0.5

Источник: «заметки.docx». Важно: часть пунктов уже сделана в v2.0.5 — заказчица
тестировала раньше; см. «Проверить (вероятно уже готово)».

## Задачи Codex

### T-G1 — Иконки вкладок по типу (Easy)
- В `TabBar` у вкладки персонажа (`kind === 'character'`) — иконка `UserRound`; убедиться,
  что таймлайн (`Clock3`), доска (`LayoutDashboard`), глава (`FileText`), персонажи-список
  (`Users`) различимы. Цель — визуальное разделение вкладок.
**Приёмка:** вкладка персонажа с человечком; типы вкладок отличаются иконками.

### T-G2 — Большая вертикальная картинка в просмотре (Easy)
- В лайтбоксе (`.image-lightbox img`, используется и в ImageStrip, и в Collage) большое
  вертикальное изображение обрезается. Задать `max-height: 92vh; max-width: 92vw; object-fit: contain`,
  чтобы помещалось целиком. Проверить кнопки ←/→/✕ поверх.
**Приёмка:** вертикальная картинка видна полностью.

### T-G3 — Библиотека помнит папку и свёрнутость при перезаходе (Medium)
- На «Библиотеке» (Shelf) раздел «Все истории» сворачивается, но при возврате на вкладку
  снова раскрыт и сбрасывается папка/прокрутка («перебрасывает на начало»). Сохранять между
  переключениями вкладок: текущую папку и состояние свёрнутости (localStorage по projectId).
  Не сбрасывать `storiesOpen` при смене папки.
**Приёмка:** ушёл с «Библиотеки» и вернулся — та же папка, та же свёрнутость.

### T-G4 — Персонажи помнят открытую папку (Medium)
- Вкладка «Персонажи»: если была открыта папка и переключиться на другую вкладку и обратно,
  возвращает на корневой экран. Сохранять текущую папку персонажей (localStorage по projectId),
  восстанавливать при повторном открытии.
**Приёмка:** открыл папку персонажей, ушёл и вернулся — папка осталась открытой.

## Senior-очередь (Claude)

- **S-G1** Сайдбар: верхняя «+» (создать историю) — через `promptStory` с выбором папки
  (сейчас выбор папки есть только в меню «+» сверху и палитре). (#1)
- **S-G2** Редактор: умная вставка v2 — всё ещё лишние пустые строки и шрифт 8/16pt при копировании
  из другого файла. Разобрать с живым буфером. (#2)
- **S-G3** Редактор: при удалении пункта в середине нумерованного списка сбивается нумерация. (#3)
- **S-G4** Редактор: Заголовок 1/2 — текст огромный, а в селекторе «16». Показывать реальный размер
  заголовка в выпадающем списке (H1≈32, H2≈25, H3≈20). (#4)
- **S-G5** Редактор: интерактивный «# стр.» в статус-баре (переход к странице) + номер страницы у
  ползунка прокрутки; поиск по страницам. (#7)
- **S-G6** Страница персонажа: описания полей свёрнуты по умолчанию + запоминать, какое поле было
  открыто (на персонажа). (#12)
- **S-G7 (обсудить)** Импорт Word: расхождение числа страниц/шрифта/полей листа. Нужны примеры. (#6)

## Проверить (вероятно уже готово в v2.0.5)
- Замена анкеты шаблоном с вопросом «заменить/дополнить» — сделано на странице персонажа (`fc58369`).
- Дерево «Персонажи» по папкам в сайдбаре (`e6c4587`).
- Цвет истории/папки в ПКМ + «→ перейти в папку» (`78b34f2`).
- Папки свёрнуты по умолчанию (`efe4859`).
- Цвет персонажа — на странице персонажа (ColorPalette), цвет папок персонажей — в панели папки.

### ФАЗА 11 — статус senior (обновление)
- ✅ S-G1 выбор папки в «+» сайдбара (`c26acbc`)
- ✅ S-G2 усилена чистка вставки (`a9fae47`) — если шрифт всё ещё кривой, нужен исходник заказчицы
- ✅ S-G3 нумерация списка при Backspace (`0f00f30`)
- ✅ S-G4 реальный размер заголовка в селекторе (`c26acbc`)
- ✅ S-G6 поля персонажа свёрнуты + запоминаются (`bb8d977`)
- ⏳ S-G5 интерактивный «# стр.»/переход по страницам — не начато
- 💬 S-G7 импорт Word — нужны примеры от заказчицы
- ✅ S-G5 навигация по страницам (клик «Стр. X/N» + бейдж при скролле) (`316ec24`)
- ✅ T-G1–T-G4 (Codex) — отревьюено, запушено (`0e50299`..`b343b86`)
- ✅ S-G7 импорт Word с форматированием (свой OOXML-парсер: шрифт/размер/начертание/
  выравнивание/красная строка/межстрочный/картинки; пустые абзацы сохраняются;
  фоллбэк mammoth) (`59eed3c`). Абзацы вплотную (.fc-prose p margin:0). Точное число
  страниц как в Word недостижимо (другой движок), но вид и объём близкие.
- ✅ Лишние пустые строки/«Стр.4/2» — поправлено (`4d199db`).
- ✅ v2.0.6/2.0.7 — размер шрифта в pt, счётчик страниц (скрытая граница), окно «Что нового».

---

# ФАЗА 12 — фидбэк заказчицы по v2.0.7

Разделение: **senior (Claude)** берёт логику редактора (S-H*), **Codex** — UI/навигацию (T-H*).
Работаем параллельно. Codex: стейджить только свои файлы, не трогать редактор (Editor.tsx,
Toolbar.tsx, *Delete.ts, FontSize.ts, PageBreak.ts) — там идут правки senior.

## Codex-очередь (T-H*)

- **T-H1** Иконки вкладок упорядочить (и сверху в TabBar, и слева в дереве):
  глава = страница (FileText), персонаж = человечек (UserRound), таймлайн = своя иконка
  (напр. GitBranch/Waypoints), доска = своя (напр. LayoutGrid/Frame). Сейчас часть вкладок
  с одинаковыми значками — нужно визуальное разделение. Иконки из lucide-react.
- **T-H2** Верхняя вкладка персонажа не обновляется при переименовании: при создании персонаж
  называется «новый персонаж» и слева, и сверху; после переименования **левая** вкладка
  меняется на имя, а **верхняя** — нет. Синхронизировать заголовок верхней вкладки с именем
  персонажа (как уже сделано для левой).
  **Приёмка:** переименовал персонажа → и левая, и верхняя вкладки показывают новое имя.
- **T-H3** Папки персонажей на вкладке «Персонажи»: переход внутрь папок через стрелочки
  (раскрытие/сворачивание), как в дереве историй слева. Сейчас навигация по папкам
  персонажей внутри вкладки неудобна.
- **T-H4** Перемещение порядка (drag-and-drop или стрелки «вверх/вниз» в ПКМ) для папок,
  подпапок, историй и персонажей — **в т.ч. на самой вкладке «Персонажи»**, а не только в
  левой панели. Уточнить с senior, если затрагивает store/reorder API.
  **Приёмка:** можно изменить порядок элементов и он сохраняется после перезапуска.

## Senior-очередь (S-H*) — редактор, делает Claude

- **S-H1** ФПО (формат по образцу): копировать ВСЁ, включая **тип блока** (обычный текст /
  заголовок 1/2/3), и применять ко **всему выделению**, а не к одному слову/букве. (#5, #6)
- **S-H2** Размер шрифта для отдельного слова: при курсоре в невыделенном слове меняется
  высота строки, а не слово; в заголовках меняется весь заголовок. Привести к Word-поведению. (#15)
- **S-H3** Backspace в нумерованном списке: уточнить ожидаемое поведение с заказчицей
  (сейчас возвращает на прошлую строку, не удаляет номер). (#4)
- **S-H4** Ctrl+A не удаляет текст, если до этого не кликнуть в лист (фокус редактора). (#5)
- **S-H5** Выделение со 2+ страницы вверх выделяет все предыдущие страницы. (#6/#8)
- **S-H6** Заголовки: различать не только размером (межстрочный h1 vs h2/h3 выровнять/осмыслить);
  при заданном размере заголовка смена 1→2→3 должна срабатывать. (#7)
- **S-H7** Тип шрифта в селекторе не отражается после импорта Word. (#8)
- **S-H8** Мелькание текста при переключении вкладок (реверстка пагинации). (#9)
- **S-H9** Гиперссылка: не подчёркивается буква «д»; добавить «убрать ссылку, не удаляя текст». (#10/#11)
- **S-H10** Поиск: если начать не с начала главы — страница не следует за найденным словом. (#12)
- **S-H11** «Это поле не прокручивается» — нужен скрин от заказчицы, что за поле. (#12, 💬)

### Статус senior (ФАЗА 12)
- ✅ S-H1 ФПО: применяем по отпусканию мыши/Shift + копируем тип блока (`d5f43cc`)
- ✅ S-H2 размер слова: при выделении красим только текст, не весь блок (`d5f43cc`)
- ✅ S-H3 пустой пункт «без номера», динамическая нумерация одним списком (`9f67218`)
- ✅ S-H4 фокус редактора при открытии — Ctrl+A без клика (`5dbd33c`)
- ✅ S-H6 смена уровня заголовка сбрасывает ручной размер + единый line-height (`cb9426a`)
- ✅ S-H7 шрифт в селекторе после импорта (матч по семейству + блок) (`cb9426a`)
- ✅ S-H8 плавное появление листа — нет «прыжка» при переключении вкладок (`b61b5e1`)
- ✅ S-H9 убрать ссылку без удаления текста + подчёркивание «д» (`5dbd33c`)
- ✅ S-H10 прокрутка к найденному слову вручную (coordsAtPos) (`cb9426a`)
- ✅ S-H11 длинные контекстные меню прокручиваются (`82c783a`)
- ⏳ S-H5 выделение со 2+ страницы вверх захватывает предыдущие — квирк координат
  пагинации (posAtCoords в межстраничном зазоре). Отложено: править рискованно,
  нужен аккуратный подход/обновление tiptap-pagination-plus.

### Статус Codex (ФАЗА 12) — отревьюено senior
- ✅ T-H1 иконки разделов (страница/человечек/Waypoints-таймлайн/LayoutGrid-доска) (`2f734f1`)
- ✅ T-H2 имя верхней вкладки персонажа синхронится при переименовании (`58e2199`)
- ✅ T-H3 дерево папок персонажей на вкладке «Персонажи» (`0351f0f`)
- ✅ T-H4 порядок папок/историй/персонажей (поле order, reorder API, DnD/стрелки),
  сохраняется между запусками (`0e2e1f1`)
Все 4 — типчек (node+web) и сборка зелёные вместе с правками senior.

### Доработка senior
- ✅ S-H9 расширен: «убрать ссылку» работает и для ссылки на главу; правый клик по
  любой ссылке → «Открыть» / «Убрать ссылку (оставить текст)» (`4d1c0cf`)

## ФАЗА 12 — добор (Codex, T-H5..T-H7)

- **T-H5** Перемещение ПАПОК в левом дереве. Сейчас drag-and-drop работает для
  историй и глав, но НЕ для папок (историй и, по аналогии, персонажей). Добавить
  DnD-переупорядочивание папок среди соседей (API `folders:reorder` уже есть из
  T-H4: `{projectId, parentId, order}`), сохранять между запусками. Перемещение
  папки внутрь другой папки (folders:move) уже есть — не ломать.
  **Приёмка:** в сайдбаре можно перетащить папку выше/ниже среди соседних, порядок
  сохраняется после перезапуска.
- **T-H6** Корзина: кнопка «Очистить корзину» (удалить всё безвозвратно). Сейчас в
  «Корзине» элементы чистятся по одному. Добавить кнопку «Очистить всё» с
  подтверждением (confirmDialog, danger), которая безвозвратно удаляет все
  элементы в корзине (истории/главы/персонажи). Если нет общего IPC — собрать из
  существующих purge (`stories.purge`/`chapters.purge`) или добавить `trash:empty`.
  **Приёмка:** кнопка очищает корзину целиком после подтверждения.
- **T-H7** Страница «Персонажи» (список папок) — сделать аккуратнее/профессиональнее.
  Сейчас папки — простые строки (см. скрин заказчицы). Предложить карточки/сетку:
  иконка папки, название, счётчик персонажей, ховер, аккуратные отступы/тени в стиле
  библиотеки историй. Также блок «Все персонажи» и «Папки» оформить как разделы.
  **Приёмка:** страница персонажей выглядит как карточки/сетка, в едином стиле с
  остальным приложением, не ломает навигацию по папкам (стрелки/открытие).

### Статус добора (ФАЗА 12) — отревьюено senior
- ✅ T-H5 перетаскивание папок в дереве (отдельный Droppable type="folder",
  folders/characterFolders.reorder) (`1b9a8a4`)
- ✅ T-H6 «Очистить корзину» — IPC trash:empty + кнопка с подтверждением (`2695ef4`)
- ✅ T-H7 редизайн страницы персонажей (карточки/сетка) (`bba6302`)
- 🔁 S-H5 v2: CSS не помог; JS-коррекция выделения на mouseUp (если голова улетела
  в начало из-за зазора — пересчёт по ближайшей строке у курсора). Нужна проверка.
Типчек (node+web) и сборка зелёные вместе с правками senior.

# ФАЗА 13 — фидбэк по v2.0.8 (Codex: T-J*, senior: S-J*)

## Codex-очередь (T-J*)
- **T-J1** Перетаскивание РАСКРЫТОЙ папки в левом дереве даёт визуальный «треш»
  (наложение/прыжки при DnD, когда папка развёрнута). С историями/главами ок.
  Починить рендер при перетаскивании развёрнутой папки (react-beautiful-dnd:
  схлопывать содержимое на время drag или не рендерить детей в draggable-клоне).
  **Приёмка:** перетаскивание раскрытой папки выглядит аккуратно, без артефактов.
- **T-J2** Расширить перемещение порядка на ВСЕ разделы: таймлайны, доски, и
  персонажей — в т.ч. на самой вкладке «Персонажи» (а не только слева). API
  reorder/порядок добавить по аналогии с T-H4 (order + *:reorder).
  **Приёмка:** можно менять порядок таймлайнов, досок, персонажей; сохраняется.
- **T-J3** Папки персонажей: раскрытие/сворачивание стрелочками на вкладке
  «Персонажи» — как в дереве историй (#11). (Похоже на T-H3, но именно внутри
  страницы «Персонажи» — проверить, что работает там.)
- **T-J4** Верхняя вкладка персонажа всё ещё не переименовывается при смене имени
  (T-H2 не добил): при создании «новый персонаж» — переименовал → слева меняется,
  СВЕРХУ нет. Досинхронизировать заголовок верхней вкладки. Проверить на реальном
  переименовании, что верхняя вкладка обновляется.

- **T-J5** Стрелка «зайти в папку» (→) для папок ПЕРСОНАЖЕЙ в левом дереве.
  У папок историй есть кнопка `.tree-goto` (ArrowRight) → `goToLibraryFolder(f.id)`,
  открывающая папку в библиотеке. У `renderCharacterFolder` (Sidebar.tsx ~792) такой
  кнопки нет — добавить такую же: по клику открыть вкладку «Персонажи» и навигировать
  ВНУТРЬ этой папки (показать её персонажей), как делает «зайти в папку» у историй.
  Нужно прокинуть целевую папку на страницу «Персонажи» (Characters.tsx) — по аналогии
  с goToLibraryFolder (store) или через localStorage/поле в сторе.
  **Приёмка:** клик по → на папке персонажей в дереве открывает вкладку «Персонажи»
  уже внутри этой папки.

### Статус добора (ФАЗА 13) — отревьюено senior
- ✅ T-J1 артефакты DnD папок: содержимое раскрытой папки схлопывается на время
  перетаскивания (`c7582dd`)
- ✅ T-J2 порядок таймлайнов/досок/персонажей (order + *:reorder, по образцу T-H4) (`fed0e05`)
- ✅ T-J3 раскрытие папок персонажей на вкладке «Персонажи» (`8b76680`)
- ✅ T-J4 верхняя вкладка персонажа берёт имя из модели live (`e5cf82f`)
- ✅ T-J5 стрелка «зайти в папку персонажей» (goToCharacterFolder, как goToLibraryFolder) (`630c2eb`)
Типчек (node+web) и сборка зелёные вместе с правками senior.

### Статус senior (ФАЗА 13)
- ✅ Меню Electron по Alt убрано; зум через transform + потолок 160%; Ctrl+A фокус
- ✅ Заголовки: переключаемый жирный, межстрочный, абсолютные размеры
- ✅ Ссылки (гипер/вики/на главу): открыть/«как обычный текст»/убрать
- ✅ ФПО не ломает нумерацию; #6 наследование шрифта документа
- ⏳ «Начать список с номера N» — не начато (нужна ли фича — уточнить)
- ⏳ S-H5 выделение со 2+ страницы — отложено (квирк координат пагинации)

# ФАЗА 14 — нумерация списков на разрыве страницы (T-K1, передано Codex)

- **T-K1** Номер пункта нумерованного списка УЕЗЖАЕТ В ПРАВЫЙ КРАЙ листа, когда
  пункт попадает на границу страницы (текст переходит на след. страницу, а номер
  «6.1.1» остаётся справа на предыдущей). Senior пробовал и зашёл в тупик —
  передаём Codex.
  Контекст/находки:
  • Нумерация самописная (CSS-счётчик), нужна для подпунктов 2.1/2.1.1 (counters)
    и «пустого пункта без номера» (data-unnumbered). Менять на родную нельзя —
    заказчица выбрала вариант с 2.1.1.
  • CSS списка: src/renderer/src/features/editor/editor.css (~287). Маркер сейчас
    через `.fc-prose ol > li > p:first-of-type::before { content: counters(fc-ol,'.') }`.
  • Пагинатор tiptap-pagination-plus на граничной строке навешивает
    `display: table; width: 100%` (правила в node_modules/tiptap-pagination-plus/
    dist/PaginationPlus.js:95-101: `*:has(>br.ProseMirror-trailingBreak:only-child)`
    и `.rm-br-decoration`). Похоже, из-за этого сгенерированный ::before-номер на
    граничной строке оказывается у правого края.
  • Пробовали: ::marker (тоже уезжал вправо), ::before absolute (номер пропадал у
    пунктов с началом на новой странице), ::before на <li> (номер на отдельной
    строке), break-inside: avoid (НЕ помог + ЗАВИСАНИЕ при зуме ~120% — откатили,
    не использовать снова!).
  Цель: номер пункта остаётся СЛЕВА (висячий отступ) и на разрыве страницы;
  подпункты 2.1/2.1.1 и «пустой пункт без номера» (Backspace) сохранить; зум не
  должен зависать. Возможные направления: переопределить для пунктов списка
  table-display от пагинатора; либо рендерить номер как реальный inline-узел через
  NodeView, а не CSS-генерируемый контент; либо патч взаимодействия с пагинатором.
  Тестировать ИМЕННО на разрыве страницы (длинный список) и проверить зум.

### Статус ФАЗА 14
- ✅ T-K1 РЕШЕНО senior: съезд номера на разрыве ушёл после отката break-inside +
  маркер на `li > p::before` (min-width). Заказчица подтвердила. Codex брать НЕ нужно.
- ✅ Зависание на ~120% на списках — причина была break-inside: avoid, откатили.
- ✅ Подпункты 2.1.1 не налезают на текст (min-width).
- ✅ Вкладка закрывается при удалении главы (applyProject прунит мёртвые вкладки).
- ✅ Контекст-меню «Переименовать/Удалить» для персонажей и папок персонажей.
